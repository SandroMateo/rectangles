<h1>Rectangle length: $rectangle.getLength()</h1>
<h1>Rectangle width: $rectangle.getWidth()</h1>
#if ($rectangle.isSquare())
  <p>Your rectangle is square!</p>
  <p>If it was a cube it's volume would be: $cube.calculateVolume()</p>
  <p>It's surface area would be: $cube.calculateSurfaceArea()</p>
#else
  <p>Your rectangle loses at being a square. He's super cool! *wink* *lensflare* *jj abrams*</p>
#end
